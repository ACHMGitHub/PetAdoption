<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.petadoption.minprogram.dataInterface.mapper.PermissionMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.petadoption.minprogram.dataInterface.entity.Permission">
        <id column="per_id" property="perId"/>
        <result column="per_url" property="perUrl"/>
        <result column="per_name" property="perName"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        per_id, per_url, per_name
    </sql>

</mapper>
